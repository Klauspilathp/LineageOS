# activemq 配置
spring.activemq.enabled=true
spring.activemq.pool.enabled=true
spring.activemq.pool.max-connections=100
spring.jms.cache.enabled=true

# 空闲连接过期时间，默认 30s
spring.activemq.pool.idle-timeout=30000
spring.activemq.broker-url=tcp://127.0.0.1:61616
spring.activemq.user=wujialong
spring.activemq.password=000000
# 是否将代理 url 保存在内存中
spring.activemq.in-memory=true
spring.activemq.close-timeout=15s
# 等待消息发送的响应时间，0 表示永远等待
spring.activemq.send-timeout=0
# 是否在回滚消息之前停止消息传递
spring.activemq.non-blocking-redelivery=false

# 是否信任所有包
# spring.activemq.packages.trust-all=
# 要信任的特定包逗号分隔列表
# spring.activemq.packages.trusted=
# 当连接请求和池满时是否阻塞。设置 false 会抛 JMSException 异常
# spring.activemq.pool.block-if-full=true
# 如果池已满，则在抛出异常前阻塞时间
# spring.activemq.pool.block-if-full-timeout=-1ms
# 是否在启动时创建连接，可以在启动时加热池
# spring.activemq.pool.create-connection-on-startup=true
# 是否用 Pooledconnectionfactory 代替普通的 ConnectionFactory
# spring.activemq.pool.enabled=false
# 连接过期时间
# spring.activemq.pool.expiry-timeout=0ms
# 每个连接的有效会话最大数目
# spring.activemq.pool.maximum-active-session-per-connection=500
# 当有 JMSException 时尝试重新连接
# spring.activemq.pool.reconnect-on-exception=true
# 在空闲连接清除线程之间运行的时间。当为负数时，没有空闲连接驱逐线程运行
# spring.activemq.pool.time-between-expiration-check=-1ms
# 是否只使用一个 MessageProducer
# spring.activemq.pool.use-anonymous-producers=true
